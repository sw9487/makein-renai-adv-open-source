# 《敗北女角太多了！》AI 戀愛 ADV：本機 Docker 安裝、LLM 與 Pony 生圖設定教學

最近製作了一套以《敗北女角太多了！》為主題的 AI 戀愛文字冒險遊戲——Makein Renai ADV。

遊戲會透過 LLM 即時產生劇情、選項、角色對話、LINE 訊息及 Twitter 動態，角色也會保留與玩家相處的記憶。若另外連接 Stable Diffusion，還能依劇情生成 CG 或讓角色在 LINE 中傳送圖片。

GitHub 專案：

[Makein Renai ADV](https://github.com/sw9487/makein-renai-adv-open-source)

> 本作是非官方、非商業同人專案，與原作者、出版社及動畫製作委員會沒有合作或授權關係。專案採 PolyForm Noncommercial License，屬於原始碼公開的非商業授權，並非 MIT 或 OSI 定義的開源授權。使用前請閱讀專案的 LICENSE 與 NOTICE。

## 一、需要準備什麼？

### 只遊玩文字內容

最低建議：

- 64 位元 Windows 10／11、macOS 或 Linux
- 雙核心以上 CPU
- 4 GB RAM，建議 8 GB
- 約 5～10 GB 可用空間
- Git
- Docker Desktop
- 可使用的 LLM API

因為主要 AI 運算由 LLM API 供應商處理，所以只玩文字內容時，不需要高階顯示卡。

### 在本機使用 Stable Diffusion／Pony 生圖

建議：

- NVIDIA 顯示卡
- 最低約 8 GB VRAM，12 GB 以上體驗較好
- 16 GB RAM，建議 32 GB
- SSD 至少預留 30～50 GB
- AUTOMATIC1111 Stable Diffusion WebUI 或相容 API
- Pony checkpoint 與對應角色 LoRA

實際顯存需求會受 checkpoint、圖片尺寸、Hires.fix 與 WebUI 最佳化設定影響。顯示卡不足時仍可關閉 Stable Diffusion，只使用文字遊戲功能。

## 二、安裝 Docker Desktop

先下載並安裝 [Docker Desktop](https://www.docker.com/products/docker-desktop/)。

Windows 使用者通常需要啟用 WSL 2。安裝完成後啟動 Docker Desktop，等待畫面顯示 Docker Engine 已經開始運作。

接著確認 Docker 指令可用：

```powershell
docker --version
docker compose version
```

如果兩個指令都有顯示版本號，就可以繼續。

## 三、下載遊戲

打開 PowerShell、Windows Terminal 或其他終端機，執行：

```powershell
git clone https://github.com/sw9487/makein-renai-adv-open-source.git
cd makein-renai-adv-open-source
```

如果沒有安裝 Git，也可以在 GitHub 頁面按下「Code → Download ZIP」，解壓縮後在該資料夾開啟終端機。

## 四、使用 Docker 在本機部署

在專案根目錄執行：

```powershell
docker compose up -d --build
```

第一次啟動需要下載映像、安裝套件及建置程式，因此會花幾分鐘。

完成後檢查服務狀態：

```powershell
docker compose ps
```

正常情況下會看到：

- `postgres`：`healthy`
- `s3`：`healthy`
- `app`：`Up`

接著用瀏覽器開啟：

- 遊戲：http://localhost:9487/
- Editor：http://localhost:9487/editor

Docker 會同時啟動三項服務：

- 遊戲與 Editor
- PostgreSQL：保存進度、記憶及系統設定
- SeaweedFS S3：保存生成或上傳的圖片

### 常用管理指令

查看遊戲日誌：

```powershell
docker compose logs -f app
```

停止遊戲但保留資料：

```powershell
docker compose stop
```

再次啟動：

```powershell
docker compose start
```

更新程式後重新建置：

```powershell
git pull
docker compose up -d --build
```

停止並移除容器，但保留存檔：

```powershell
docker compose down
```

請不要隨便執行：

```powershell
docker compose down -v
```

加上 `-v` 會刪除 PostgreSQL 與 S3 volumes，遊戲進度、設定和生成圖片都可能一起消失。

## 五、設定 LLM API

這個遊戲必須先設定 LLM，因為劇情、選項、對話、LINE 與 Twitter 都會使用 AI 生成。

開啟：

http://localhost:9487/editor

進入「系統設定」，找到 AI／LLM 設定區，填入：

- API URL
- API Key
- Model

### API URL 格式

遊戲使用 OpenAI 相容的 Chat Completions API，URL 通常需要填到 `/v1`，例如：

```text
https://api.example.com/v1
```

不要填完整的 `/chat/completions` 路徑。

### 模型需求

建議選擇同時支援以下能力的模型：

- 圖片辨識／Vision
- Tool calling／Function calling
- OpenAI 相容 Chat Completions
- 較長的上下文長度
- 穩定輸出結構化工具參數

按下「儲存 AI 設定」時，系統會用一次 API 請求驗證圖片辨識與 tool call。只有驗證成功後才會保存設定。

如果失敗，請依序檢查：

1. API URL 是否填到 `/v1`
2. API Key 是否正確
3. 模型名稱是否存在於 `/v1/models`
4. 模型是否真的支援 Vision 與 tool calling
5. API 供應商是否限制來源地區或帳號額度

API Key 只會由遊戲伺服器使用，不會直接傳到瀏覽器前端。

## 六、設定 Stable Diffusion 與 Pony

Stable Diffusion 是選用功能。沒有設定 SD 仍然可以正常進行文字劇情與對話。

本專案使用 AUTOMATIC1111 相容 API，推薦的 Pony checkpoint 是：

[Zuki Clean Anime Mix](https://civitai.com/models/880541/zuki-clean-anime-mix)

模型與 LoRA 不包含在 GitHub 專案內，需自行下載，並遵守各下載頁面的授權與使用限制。

### 1. 啟動 Stable Diffusion WebUI API

AUTOMATIC1111 必須以 `--api` 啟動。

如果遊戲運行在 Docker，而 WebUI 運行在 Windows 主機，WebUI 還需要允許 Docker 容器連線。可在 `webui-user.bat` 設定：

```bat
set COMMANDLINE_ARGS=--api --listen
```

然後啟動 WebUI。

`--listen` 可能讓同一區域網路內的其他裝置連線，請確認 Windows 防火牆設定，不要直接把 7860 port 暴露到公網。

### 2. 安裝 Pony checkpoint

將下載的 checkpoint 放入：

```text
stable-diffusion-webui/models/Stable-diffusion/Pony/
```

如果 WebUI 無法辨識子資料夾，也可以直接放在：

```text
stable-diffusion-webui/models/Stable-diffusion/
```

重新啟動 WebUI或刷新 checkpoint 清單，並記下介面顯示的完整 checkpoint 名稱。

### 3. 安裝角色 LoRA

專案使用 Pony LoRA 時，建議放在：

```text
stable-diffusion-webui/models/Lora/Pony/
```

請保留下載包內的角色子資料夾與檔名。不同模型家族若出現同名 LoRA，建議加上 `Pony_` 或 `Illustrious_` 前綴，避免 WebUI 載入錯誤檔案。

### 4. 在遊戲 Editor 設定 SD

進入：

```text
http://localhost:9487/editor
```

在「系統設定 → Stable Diffusion」中設定：

- 啟用 Stable Diffusion
- 模型類型：`Pony`
- Stable Diffusion URL
- Checkpoint 完整名稱
- Bearer API Key：本機沒有驗證時留白
- 是否啟用自動劇情生圖

如果遊戲使用 Docker、WebUI 位於同一台 Windows 或 macOS 電腦，URL 通常填：

```text
http://host.docker.internal:7860
```

不要填 `http://localhost:7860`，因為 Docker 容器中的 `localhost` 指的是遊戲容器本身，不是主機上的 WebUI。

如果遊戲不是跑在 Docker，而是直接在本機執行，則通常可填：

```text
http://127.0.0.1:7860
```

Pony 的專案推薦預設值為：

- Steps：30
- CFG：6
- Clip skip：2
- Hires 倍率：1.5
- Denoising：0.45
- Sampler：Euler a
- Scheduler：Automatic
- Upscaler：R-ESRGAN 4x+ Anime6B

儲存後，建議先使用手動「產生劇情圖片」測試。確認成功後，再考慮開啟自動劇情生圖。

## 七、開始遊戲

LLM 驗證成功後，按下 Editor 的「返回遊戲」，或者直接開啟：

http://localhost:9487/

選擇「開始新故事」即可進入遊戲。

遊戲進度會自動保存在 PostgreSQL，不需要手動建立帳號。瀏覽器 cookie 只用來識別玩家；只要保留相同 cookie，重新整理、關閉瀏覽器或重新啟動 Docker 後，仍會載入原本的進度。

如果清除 cookie，系統會把你視為另一位玩家，但舊資料不會因此自動從資料庫刪除。

## 八、遊戲有哪些功能？

### AI 動態劇情

劇情、角色反應和選項會根據以下資訊即時生成：

- 當前日期與時段
- 玩家所在位置
- 角色身分與個性
- 好感度
- 已發生事件
- 角色對玩家的記憶
- 玩家先前做過的選擇

同一事件在不同關係及遊戲狀態下，可能出現不同的對話與反應。

### 戀愛路線與好感度

遊戲包含多名角色及六條戀愛路線。玩家的選擇、日常互動、對話與事件結果會影響好感度、信任及後續發展。

遊戲具有日期、平日、週末、假期、學年與畢業等時間系統，不同角色也有各自的登場時期與活動範圍。

### 面對面自由對話

遇到角色後可以進行自由對話。角色會依個性、關係、場景及過去記憶回答，而不只是播放固定台詞。

角色具有多種表情立繪，AI 可根據當下情緒切換表情。

### LINE 系統

與角色交換聯絡方式後即可使用 LINE：

- 主動傳送訊息
- 收到角色的主動訊息
- 已讀或不回覆
- 延續先前的對話話題
- 發出約會邀請
- 在啟用 SD 時接收角色照片

角色是否回覆會依情境決定，不保證每一則訊息都立即得到回答。

### 遊戲內 Twitter

遊戲內建一套虛構社群系統，並不連接真實 Twitter。

玩家和角色可以：

- 發文與回覆
- 按讚
- 轉發及引用
- 追蹤與取消追蹤
- 使用提及與主題標籤
- 接收通知
- 處理私人帳號的追蹤請求
- 查看角色與公共帳號的動態

角色也可能根據遊戲事件及彼此關係，自主發文或互動。

### Stable Diffusion 劇情 CG

啟用 SD 後可生成：

- 有人物的劇情 CG
- 無人物的物件圖片
- 場景與風景圖
- LINE 生活照
- Twitter 主貼文配圖

生圖失敗通常不會阻止文字劇情保存，可以修正 SD 設定後重試。

### 存檔、回顧與收藏

遊戲提供：

- 自動存檔
- 六個手動存檔欄位
- 事件回顧
- CG 收藏
- 結局收藏
- 角色獨立記憶

### 內容 Editor

Editor 不只是 API 設定頁，也可以修改：

- 人物資料
- 角色 Prompt
- 立繪與表情
- 場景及相遇設定
- 事件劇本
- 好感度
- CG
- 素材書庫
- 遊戲系統設定

上傳或生成的圖片會保存到 S3 儲存空間，不會直接寫入 Git 專案目錄。

## 九、常見問題

### 開啟 9487 沒有畫面

先檢查：

```powershell
docker compose ps
docker compose logs -f app
```

也要確認 9487 port 沒有被其他程式占用。

### LLM 顯示無法取得模型清單

確認：

- API URL 以 `/v1` 結尾
- API Key 有效
- 模型供應商提供 `/v1/models`
- Docker 容器可以連上網路
- 模型名稱與供應商回傳名稱完全一致

### Stable Diffusion 回覆 404

確認 WebUI 是以 `--api` 啟動，並嘗試使用：

```text
http://host.docker.internal:7860
```

也可以直接填完整路徑：

```text
http://host.docker.internal:7860/sdapi/v1/txt2img
```

### Stable Diffusion 出現 `NansException`

可以先在 WebUI 啟用：

```text
Upcast cross attention layer to float32
```

如果仍然失敗，再考慮以以下參數啟動：

```text
--api --listen --no-half
```

`--no-half` 會增加顯存需求，建議只在確實遇到相容性問題時使用。

## 結語

Makein Renai ADV 的核心不是單純把固定劇本交給 AI 改寫，而是讓日期、地點、角色記憶、好感度、LINE、Twitter 與事件系統共同影響每一次互動。

只要有 Docker Desktop 和合適的 LLM API，就可以先體驗完整的文字冒險；如果電腦有足夠的 NVIDIA 顯示卡，再接上 Pony Stable Diffusion，就能進一步生成劇情 CG 和角色生活照。

專案與完整安裝文件：

[https://github.com/sw9487/makein-renai-adv-open-source](https://github.com/sw9487/makein-renai-adv-open-source)

安裝及功能資訊依專案目前的 [README](https://github.com/sw9487/makein-renai-adv-open-source#readme) 與 [Stable Diffusion 文件](https://github.com/sw9487/makein-renai-adv-open-source/blob/main/docs/stable-diffusion.md) 整理。